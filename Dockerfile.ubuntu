FROM ubuntu:22.04

RUN apt-get update && apt-get install -y build-essential git

WORKDIR /home/jaysh

RUN git clone https://github.com/jayshozie/dotfiles/dev.git && cd dotfiles

RUN ./ubuntu-install && ./run/dev
